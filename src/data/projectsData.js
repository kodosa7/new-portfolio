/**
 * Portfolio Projects Data (Re-exported from locales for backwards-compatibility)
 */

import { en } from "../locales/en";

export const digitalProjects = en.digitalProjects;
export const creativeProjects = en.creativeProjects;
